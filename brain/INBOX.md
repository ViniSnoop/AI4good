# inbox
> zero friction. thoughts. no taxonomy. no formating. handle duplications.
> triage with `/inbox`: each entry routed to a goal, task, ref, project doc, draft — or deleted.
>
> signal the route preemptively (optional — agent infers if omitted):
> `goal` · `task: today`/`week`/`month`/`backlog` · `ref` · `proj: <name>` · `draft` · `delete`

---

<!-- add entries below, newest first -->

proj: workspace-os — bug no context gate: ele lê caminhos de dentro do *corpo da mensagem de commit* (heredoc) e trata como arquivo que o comando toca. Citar `core/tools/web/fetch` numa mensagem de commit exigiu carregar o CONTEXT.md daquele subtree. Falso positivo: o gate deveria olhar o comando, não o texto do heredoc.

proj: workspace-os — `core/SPECS.md` cruzou o sinal de tamanho (451 linhas, 17 ADs) ao ganhar AD-15/16/17. Não splitei no meio da sessão pra não espalhar sem regra, mas o arquivo já mistura tools/auth/flows/features/prosa-sempre-carregada/dúvida/delegação. Candidato a split por concern — decidir o eixo antes de cortar.

tive algumas reuniões com alunos hoje, quero que todos eles usem algum setup com HARNESS + uma versão desse workspace que contemple pelo menos o ramo da pesquisa e produção de artigos. daí quero preparar um prompt pra eles, em portugues, pra eles jogarem no harness que eles usam e aí junto com o harness eles decidirem o que querem aproveitar desse workspace e como fazer.

muita coisa em português nos nossos arquivos do WOS, a ideia é que somente o meu texto é em portugues, as conversas nas sessões, mas o resto, os docs, tudo em inglês inclusive pra economizar tokens.