# inbox
> zero friction. thoughts. no taxonomy. no formating. handle duplications.
> triage with `/inbox`: each entry routed to a goal, task, ref, project doc, draft — or deleted.
>
> signal the route preemptively (optional — agent infers if omitted):
> `goal` · `task: today`/`week`/`month`/`backlog` · `ref` · `proj: <name>` · `draft` · `delete`

---

<!-- add entries below, newest first -->

`code/SPEC-DRIVE.md` is gitignored (`.gitignore:37`, the `code/*` rule) while its first-level siblings `code/VERIFY.md` and `code/SPECS.md` are tracked. Edits to it never leave the machine. Either it earns tracking like the other two, or it is not a real workspace doc — found 2026-08-15 while repointing SETUP.md references.

`README.md` now lists the workspace's capabilities grouped by what each buys you (navigation / restraint / drift control / cost) — that is a first pass at the entry below, done 2026-08-15. Check whether it is the organization you wanted before the installer question is picked up.

BEFORE creating an installer for the workspace I think a good thing we can do is to systematically list what the workspace is, what are the features. we have different levels of features, I would like to see this organized first.

does entropy.md sits on the right place? and should it have an UPPERCASE name?

can we reduce the entropy? is there something we can do to reduce it?

we have a lot of huge .md files. this was a concern before and I am pointing it again. I known we passed through this check before, I am just warning that we may have to do that again.

I feel we may have to audit our .md files to see if they all bring relevant things. SCHEMA.md for instance still talks about a transition step of some old names as if we were not transitioned/fixed those yet... I mean, I am thinking maybe we deserve a full audit to CUT / REDUCE whatever isn't relevant / important.

CONTEXT.md files should be really thin. some are not.